
add_executable(refresh-purged refresh-purged.c)
target_link_libraries(refresh-purged utils)

add_executable(explain explain.c)
target_link_libraries(explain utils)

link_directories (${CADO_NFS_BINARY_DIR}/ffs/f2)
link_directories (${CADO_NFS_BINARY_DIR}/ffs/f3)
include_directories (${CADO_NFS_SOURCE_DIR}/ffs)

#link_directories (${CADO_NFS_BINARY_DIR}/gf2x/.libs)

#add_executable(purge_heap purge_heap.c)
#target_link_libraries(purge_heap utils pthread)

add_executable(purge-nfs-dl EXCLUDE_FROM_ALL purge.c)
target_link_libraries(purge-nfs-dl utils pthread)
set_property(TARGET purge-nfs-dl APPEND PROPERTY COMPILE_FLAGS "-DFOR_NFS_DL")
add_dependencies(purge-nfs-dl antebuffer)

add_executable(purge-ffs-f2 EXCLUDE_FROM_ALL purge.c utils_ffs.c)
#target_link_libraries(purge-ffs-f2 utils pthread libfppol-f2.a gf2x)
target_link_libraries(purge-ffs-f2 utils pthread libfppol-f2.a)
set_property(TARGET purge-ffs-f2 APPEND PROPERTY COMPILE_FLAGS "-DFOR_FFS -DUSE_F2")
#add_dependencies(purge-ffs-f2 gf2x-build)
add_dependencies(purge-ffs-f2 antebuffer)

add_executable(purge-ffs-f3 EXCLUDE_FROM_ALL purge.c utils_ffs.c)
target_link_libraries(purge-ffs-f3 utils pthread libfppol-f3.a)
set_property(TARGET purge-ffs-f3 APPEND PROPERTY COMPILE_FLAGS "-DFOR_FFS -DUSE_F3")
add_dependencies(purge-ffs-f3 antebuffer)

add_executable(merge-ffs EXCLUDE_FROM_ALL merge.c markowitz.c mst.c report.c merge_mono.c filter_matrix.c sparse.c utils_ffs.c)
#here we do not care about the characteristic because we only use non specific
#functions
target_link_libraries(merge-ffs utils libfppol-f3.a)
set_property(TARGET merge-ffs APPEND PROPERTY COMPILE_FLAGS "-DFOR_FFS -DUSE_F3")

add_executable(replay-ffs EXCLUDE_FROM_ALL replay.c sparse.c utils_ffs.c)
#here we do not care about the characteristic because we only use non specific
#functions
target_link_libraries(replay-ffs utils libfppol-f3.a)
set_property(TARGET replay-ffs APPEND PROPERTY COMPILE_FLAGS "-DFOR_FFS -DUSE_F3")
# vim: ft=cmake:
