
add_executable(cachesize EXCLUDE_FROM_ALL cachesize.c cachesize_cpuid.c
    cachesize_guess.c timing.cpp memusage.c)

add_executable(qrange_to_qlist qrange_to_qlist.c)
target_link_libraries(qrange_to_qlist utils)
install(TARGETS qrange_to_qlist RUNTIME DESTINATION bin/utils)

# vim: set ft=cmake:
